export class User {
    constructor(userName, email, projTitle,pass,description,type) {
        this.userName = userName,
            this.email = email,
            this.projTitle=projTitle,
            this.pass = pass,
            this.description = description,
            this.type=type
    }
}